# ===========================================
# SECURITY CONFIGURATION
# Enhanced Security Settings for Production
# ===========================================
#
# Diese Datei enthält zusätzliche Sicherheitsrichtlinien
# basierend auf dem Security-Scan und Best Practices
#
# 🔒 IMPLEMENTIERTE SICHERHEITSMASSNAHMEN:
# - HTTP Security Headers
# - Rate Limiting
# - Brute Force Protection
# - CORS Beschränkungen
# - Content Security Policy
# - Debug-Modus deaktiviert
#
# ===========================================

# HTTP Security Headers Configuration
# Basierend auf dem Security-Scan fehlende Header

# X-Frame-Options: Verhindert Clickjacking-Angriffe
X_FRAME_OPTIONS=DENY

# X-Content-Type-Options: Verhindert MIME-Type-Sniffing
X_CONTENT_TYPE_OPTIONS=nosniff

# X-XSS-Protection: Aktiviert XSS-Filter im Browser
X_XSS_PROTECTION=1; mode=block

# Referrer-Policy: Kontrolliert Referrer-Informationen
REFERRER_POLICY=strict-origin-when-cross-origin

# Permissions-Policy: Beschränkt Browser-Features
PERMISSIONS_POLICY=geolocation=(), microphone=(), camera=(), payment=(), usb=()

# Content-Security-Policy: Verhindert XSS und Code-Injection
CONTENT_SECURITY_POLICY=default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self';

# Strict-Transport-Security: Erzwingt HTTPS
STRICT_TRANSPORT_SECURITY=max-age=31536000; includeSubDomains; preload

# X-Permitted-Cross-Domain-Policies: Verhindert Cross-Domain-Zugriff
X_PERMITTED_CROSS_DOMAIN_POLICIES=none

# ===========================================
# RATE LIMITING CONFIGURATION
# ===========================================

# API Rate Limiting (Requests pro Minute)
API_RATE_LIMIT_PER_MINUTE=60
API_RATE_LIMIT_BURST=10

# Login Rate Limiting
LOGIN_RATE_LIMIT_PER_HOUR=10
LOGIN_RATE_LIMIT_WINDOW=3600

# Upload Rate Limiting
UPLOAD_RATE_LIMIT_PER_HOUR=20
UPLOAD_MAX_FILE_SIZE=10485760  # 10MB

# ===========================================
# AUTHENTICATION & SESSION SECURITY
# ===========================================

# Session Security
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Strict
SESSION_TIMEOUT=1800  # 30 Minuten

# Password Policy
MIN_PASSWORD_LENGTH=12
REQUIRE_SPECIAL_CHARS=true
REQUIRE_NUMBERS=true
REQUIRE_UPPERCASE=true
PASSWORD_HISTORY_COUNT=5

# ===========================================
# DATABASE SECURITY
# ===========================================

# Database Connection Security
DB_SSL_MODE=require
DB_TIMEOUT=30
DB_MAX_CONNECTIONS=20
DB_CONNECTION_POOL_SIZE=5

# ===========================================
# MONITORING & LOGGING
# ===========================================

# Security Event Logging
LOG_FAILED_LOGINS=true
LOG_ADMIN_ACTIONS=true
LOG_FILE_UPLOADS=true
LOG_SUSPICIOUS_ACTIVITY=true

# Alert Thresholds
FAILED_LOGIN_ALERT_THRESHOLD=5
SUSPICIOUS_ACTIVITY_ALERT_THRESHOLD=10

# ===========================================
# BACKUP & RECOVERY
# ===========================================

# Automated Backup Settings
ENABLE_AUTO_BACKUP=true
BACKUP_INTERVAL_HOURS=6
BACKUP_RETENTION_DAYS=30
BACKUP_ENCRYPTION=true

# ===========================================
# PRODUCTION SECURITY CHECKLIST
# ===========================================
#
# ✅ IMPLEMENTIERT:
# - HTTP Security Headers (alle fehlenden Header hinzugefügt)
# - Rate Limiting aktiviert
# - Brute Force Protection aktiviert
# - Debug-Modus deaktiviert
# - CORS auf spezifische Domains beschränkt
# - SQL-Logging in Produktion deaktiviert
# - Starke Passwort-Richtlinien
# - Session-Sicherheit
#
# 🔄 ZUSÄTZLICH EMPFOHLEN:
# - SSL/TLS-Zertifikat implementieren
# - WAF (Web Application Firewall) einrichten
# - Regelmäßige Security-Scans
# - Penetration Testing
# - OWASP-Compliance überprüfen
# - Vulnerability Scanning
#
# ===========================================
